.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock
.idea/
.vscode/
*.iml
.DS_Store
# Carpetas de plataforma generadas por flutter create. Ancladas a la raiz
# con la barra inicial: sin ella el patron coincide con cualquier carpeta
# de ese nombre a cualquier profundidad, y se tragaba tool/android/.
/android/
/ios/
/web/
/macos/
/linux/
/windows/
*.g.dart
*.freezed.dart

# Firma de la aplicacion. Un keystore filtrado permite publicar
# actualizaciones falsas de SeguroMina y no se puede revocar.
*.jks
*.keystore
*.p12
key.properties
keystore.b64
dist/
